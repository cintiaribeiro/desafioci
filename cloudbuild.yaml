steps:

- id: "Rodando Testes"
  name: 'gcr.io/cloud-builders/go'
  args: ['test', './src/', '-v']

- id: "Criando Imagem"
  name: "gcr.io/cloud-builders/docker"
  args:
  - 'build'
  - '--build-arg'
  - 'version=1.0'
  - '-t'
  - 'gcr.io/$PROJECT_ID/soma:latest'
  - '.'
images:
- 'gcr.io/$PROJECT_ID/soma:latest'